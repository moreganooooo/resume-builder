# Handoff — 2026-08-21 (resume-builder)

## Where things stand

Branch `main`, **3 commits ahead of `origin/main`** and NOT yet pushed:
`3bd320c7`, `8e1ab3df`, `fe51574b`. Morgan has pushed her own commits by
hand before; do not push without asking.

Quality gates all green as of the last run: unittest 2223 pass,
pylint 10.00/10, bandit 0, Node tests 52 pass.

## Just completed

1. **Liveness sweep** ran clean: 901s / 422 candidates.
   `active 268 · likely_active 21 · expired 35 · uncertain 98 · moved 35`.
   Zero hash-label rows, zero `unparseable` failures, checkpoint cleared
   on the success path (results genuinely persisted — the previous run
   checked 812 and saved nothing).
2. **Status reconciliation applied.** `scripts/reconcile_jd_status.py
   --apply` corrected 47 rows that claimed `pending` while their JD file
   sat in `expired/`. Backup at
   `profiles/Morgan/data.db.backup-20260821-205118`.
   `count_active_roles()` went 436 -> **399**.

## The count question, answered (don't re-derive this)

`436`/`399` and `737` were never in conflict — they count disjoint sets:

- **399** = pending AND evaluated. This is `picker.count_active_roles()`,
  the single definition, shown in the CLI banner and both dashboard
  screens. Correct.
- **~1,351** = pending and UNevaluated (the backlog). Of these, ~710 have
  a real JD file in `jds/Morgan/`; ~1,337 are database-only hash-keyed
  rows from board scans. The `737` figure quoted earlier was the
  file-backed slice of this at the time.

Current data.db shape: 1,876 rows / 1,796 pending / 80 expired.
No duplicate rows, no blank or errored rows. The 291 hash-keyed evaluated
rows are legitimate scan-sourced jobs; 129 of them correctly dedupe
against file-backed twins via `jd_manager.compute_job_key`.

## Next steps, highest value first

1. **Surface the unevaluated backlog.** The banner reports 399 and says
   nothing about the 1,351 awaiting evaluation. That silence is what made
   the two numbers read as a bug. Add a second line to the banner (and the
   dashboard header) alongside `count_active_roles()`. Follow the existing
   pattern: one function in `picker.py`, called by every surface, so a
   second definition can't drift in. Note `cli_art` imports `picker`
   lazily inside the function — `picker` imports `cli_art`, so a
   module-level import is circular.
2. **Evaluate the backlog.** `resume evaluate` (NOT a rescore). Cost
   scales with count — 1,351 is a lot of API calls, so consider scoring
   only the file-backed subset first, or adding a cap.
3. **Investigate the 98 `uncertain` liveness verdicts** (23% of the sweep).
   Worth checking whether those are genuinely ambiguous pages or a
   checker heuristic being too conservative.
4. **GitHub CI / Dependabot.** `gh` CLI is NOT installed here — either
   `brew install gh` or check in-browser. Expect unmergeable numpy bump
   PRs: `python-jobspy` hard-pins `numpy==1.26.3`.

## Open / unexplained

- **Empty-stdout failure at 812 candidates** (`⚠ Liveness check produced
  unparseable output`). Root cause still unknown. Did NOT reproduce at
  422. The checkpoint + `_results_from_progress()` recovery path in
  `scripts/liveness.py` now means it can't cost a whole run, but the
  underlying cause was never found.

## Constraints to preserve

- API keys live in the active profile's own `.env`
  (`profiles/<name>/.env`), never a project-root `.env`. A permission
  request to read `.env` was DENIED — do not route around it. Never ask
  Morgan to paste keys into chat.
- Playwright is pinned to an exact `1.61.1` — macOS 12 support. Do not
  loosen to a caret.
- Personal values (location, radius, salary floor) must be
  user-configurable with an in-app control, never hardcoded.
