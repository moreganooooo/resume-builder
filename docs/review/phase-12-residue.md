# Phase 12 — Remaining unowned residue: keep, wire up, or delete

Run 2026-08-05, Sonnet 5. Inventory pass over `PLAN.md`'s Phase 12 ownership
table (`PLAN.md:712-737`) — everything Phase 9's mechanical residue check
found unowned and did not assign to Phase 10 or 11. No code changes, per the
plan's operating rules. **B` items added to `phase-9-backlog.md`: B55–B59**
(next available ID was B55, per the parent task).

Runtime evidence over speculation, per the plan's conventions: every "live"
call below is backed by a `grep` for the reachability chain, a test run, or
(for the dashboard's data layer) a constructed real-format fixture actually
parsed by the Go code — not just a read of the source.

## Disposition table

| Path | Live / dead / half-wired | Disposition | Evidence |
|---|---|---|---|
| `e2e/example.spec.ts`, `playwright.config.ts` | **Dead.** Unedited `npm init playwright@latest` output — both tests call `page.goto('https://playwright.dev/')` and assert on Playwright's own site, not this app. | **Delete.** | `git log --oneline -- e2e/ playwright.config.ts` → one commit (`4984907c`), the commit that added them, nothing since. `playwright.config.ts` has no `baseURL` and a commented-out `webServer` block — never pointed at this repo. `package.json`'s `"scripts"` is `{}` — no `test` entry invokes it. `.github/` has no workflow (only `dependabot.yml`). `node_modules/` doesn't exist (see B15), so `npx playwright test` can't even resolve `@playwright/test` today. This app also has no browser-navigable UI to e2e-test — the only "web" surface is a rendered PDF/HTML file Chromium renders as a print target, not a served page — so there's no natural target to wire this scaffold up *to*. Filed as **B55**. |
| `dashboard/` non-visual Go code (`internal/data/career.go`, `internal/data/derive.go`, `internal/model/career.go`) | **Live**, verified against real output, not just read. | **Keep.** | `go test ./...` (from `dashboard/`) passes: `internal/data` and `internal/ui/screens` both `ok`. More directly: no `data/*/applications.md` exists anywhere in this repo today (`morgan`'s completed-JD directory is empty — every resume so far came from `resume sample`, which skips the tracker write, per Phase 11/B61), so this code's real-format parsing path has *never run in this repo's life* until this review exercised it. Built a fixture via the actual writer — `jd_manager.append_application_row()` twice, producing a real 10-column `resume-builder`-shape `applications.md` — then ran `ParseApplications()` against it from a temporary `_test.go` inside `internal/data/` (removed after; `git status --short dashboard/` confirms clean). Result: both rows parsed correctly — `Score: 4.2`, `JobURL` correctly extracted via `reApplyLink` from the `[Apply](url)` cell, `Notes: "[Strong pursue]"`, `HasPDF: true/false` both correct. The 10-vs-9-column branch (`career.go:122-159`), the `reApplyLink` regex (career-ops's report-link format doesn't apply here), and `ComputeMetrics`/`ComputeProgressMetrics` are all real and correct for this app's actual output shape, not just inherited career-ops generality. `main.go:36-41,154-198` and `scripts/dashboard.py` wire it correctly: the Python shim checks for `data/<profile>/applications.md` before invoking `go run . -path <data_dir>`, matching what `jd_manager.APPLICATIONS_MD`/`append_application_row()` (called from `orchestrator.py:3081`) actually writes. No defect found; this is a coverage gap that was closed by this review, not a product bug — `career_test.go`'s existing unit tests only exercise the career-ops-shape 9-column format, so the 10-column path had no *committed* test, but it works. |
| `scripts/archive/` (5 files) | **Dead**, confirmed. | **Delete.** | `grep -rn` across every tracked `.py` file for each module name (`backfill_cluster_ids`, `detect_blank_scores`, `fix`, `merge_queue_to_cluster_map`, `rewrite_bullets_backup`) returns no import and no `archive/`-relative reference anywhere outside the files themselves (the one `fix` hit in `profile_paths.py` is an unrelated docstring mentioning `fixed_content`, not this file). `git log` confirms deliberate archiving (`c5abd8be`, `d740e8da`, `063b6ba1` — "Archiving files not needed in the future"). `diff`'d `archive/detect_blank_scores.py` against the live `scripts/detect_blank_scores.py`: they've diverged completely — different purpose (rewrite-queue population vs. a blank-score summary report), different CSV paths, different CLI. Confirms and quantifies Phase 6's "drifted" note. Filed as **B57**. |
| `package.json`, `package-lock.json`, `requirements.txt` | **Half-wired / minor drift.** Manifests are internally consistent; one dependency is orphaned. | **Keep, with two small fixes.** | `package.json`'s `dependencies.playwright` (`^1.61.1`) and `devDependencies["@playwright/test"]` (`^1.62.0`) both resolve to `1.62.0` in `package-lock.json` (`node_modules/playwright`, `node_modules/@playwright/test`, `node_modules/playwright-core` all `1.62.0`) — the lock file itself is coherent; the mismatch B15 already tracks is that `node_modules/` was never materialized in this repo (`npm install` never run), so the *actually loaded* version at runtime is a stray `~/node_modules` install at `1.60.0`. No new finding there beyond B15. `requirements.txt`: checked every package for a real import (`grep -rn "import <module>"` across `scripts/`, `resume-engine/`, `tests/`). All 19 are used except `webdriver-manager` — zero direct imports and no transitive requirement from anything else in the file (`pip show webdriver-manager` → no `Required-by`; contrast `selenium`, also unimported directly but a real transitive dependency of `linkedin-jobs-scraper`). Filed as **B58**. Once `@playwright/test` is dropped per B55, `package.json` no longer looks like a test project. |
| `.github/dependabot.yml` | **Half-wired — present but non-functional.** | **Wire up.** | `.github/dependabot.yml:8` is `package-ecosystem: ""` — the literal unfilled placeholder from GitHub's scaffold template, never replaced. An empty `package-ecosystem` fails Dependabot's schema validation, so the sole `updates:` entry does nothing; functionally, dependency updates are configured for **zero** of the three ecosystems this repo actually has (pip via `requirements.txt`, npm via `package.json`, Go via `dashboard/go.mod`). Nothing acts on what it opens because it opens nothing. Filed as **B56**. |
| `profiles/morgan/board_scanner/*.yml` (`scan_filters.yml`, `search_queries.yml`, `tracked_companies.yml`) | **Live**, all three wired and read on every scan; one real self-contradiction. | **Keep, with one small fix.** | All three `yaml.safe_load` cleanly and are genuinely read: `scan_boards.py:129` (`scan_filters.yml`, via `_load_filters()`/`_passes_title_filter()`/`_passes_location_filter()`), `scan_ats.py:94` (`search_queries.yml`, 68 real hand-curated Brave-search sweep queries — not the empty scaffold; that emptiness (`P7`'s B34) is the *new-profile default* written by `bootstrap_bullet_bank.py:111`, a different file from this committed one), `scan_ats.py:88` (`tracked_companies.yml`). Real defect found in `scan_filters.yml`: `title_filter.positive` and `title_filter.negative` both contain `Curriculum` (indices 47 and 210). `scan_boards._passes_title_filter()` (`scan_boards.py:135-147`) checks `negative` unconditionally after `positive` with no precedence rule for an overlapping term, and a negative hit always rejects — so any title containing "curriculum" is silently dropped from every scan despite being listed as a wanted signal. Also 8 duplicate entries within `positive` (of 104) and 3 within `negative` (of 332) — harmless, sloppy. Filed as **B59**. |
| `fixtures/sample_jd.txt` | **Live and representative.** | **Keep.** | This is the real Abnormal AI / Content Strategist JD that every other phase's findings about the shipped resume/cover-letter reference (`P3`, `P8`, `P10` all analyze output generated from this exact fixture). `git log` shows one commit (`62111b05`, "Add resume sample: a permanent QA fixture"), never touched since — not stale, just stable. 881 words, well-formed JSON matching the real JobRight export shape `orchestrator.py` parses, with a substantive `description`, `qualifications`, `core_responsibilities` — a real, non-trivial JD, not a placeholder. No staleness signal: the pipeline it drives hasn't changed shape since (still JSON-JD-in, resume/cover-letter-PDF-out), and it continues to surface real defects (B1's ligature corruption, B28/B29's Summary and pronoun findings) when run today. |
| `docs/superpowers/`, `ImprovementConcepts/`, `.vscode/`, `.hintrc`, `.claude/`, `IDEAS*.md`, `resume_example.pdf`, `rewrite_bullets_fixes.md` | Historical/editorial. | **Out of scope.** | Per `PLAN.md:729`'s explicit instruction: recorded, not investigated. Confirmed all listed paths exist (`.claude`, `.hintrc`, `.vscode`, `IDEAS.md`, `IDEAS_ARCHIVE.md`, `ImprovementConcepts`, `docs/superpowers`, `resume_example.pdf`, `rewrite_bullets_fixes.md`) so nothing here is itself missing or needs a Phase 9-style "unowned file" claim. Phase 5 already scanned `IDEAS.md`/`ImprovementConcepts/` for cheap-to-build ideas per its own remit. No further action taken. |

## New backlog items

Appended to `docs/review/phase-9-backlog.md`'s Tier 3 (minors/hygiene),
matching that section's existing format:

- **B55** — Playwright e2e scaffold is dead, points at `playwright.dev`, and
  is unreachable from every angle checked (no test script, no CI, no
  `node_modules/`). Delete `e2e/` + `playwright.config.ts` + the
  `@playwright/test` devDependency.
- **B56** — `.github/dependabot.yml`'s `package-ecosystem` is the unfilled
  `""` placeholder; it updates none of pip/npm/gomod. Needs three real
  `updates:` entries.
- **B57** — `scripts/archive/` confirmed dead (zero imports repo-wide,
  deliberately archived) and `detect_blank_scores.py`'s drift from the live
  copy is quantified. Delete the directory.
- **B58** — `webdriver-manager` in `requirements.txt` is an orphaned
  dependency: no direct import, no transitive requirer. Remove the line.
- **B59** — `scan_filters.yml`'s `title_filter` lists `Curriculum` in both
  `positive` and `negative`; the negative list always wins, so the positive
  entry is dead and curriculum-adjacent titles are silently dropped from
  every scan. Drop it from `negative`; dedupe both arrays while there.

No blockers or majors among these five — all minor, goal 5 (or goal 1 for
B59), consistent with Phase 12's low-cost inventory remit.
