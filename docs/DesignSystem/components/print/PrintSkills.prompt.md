The skills block: one category per line, immediately after the summary.

```jsx
<PrintSkills groups={[{ label: "Brand", items: ["Positioning", "Naming", "Voice"] }]} />
```

Never join categories with pipes on one flow — the separators end up stranded at line wraps.
