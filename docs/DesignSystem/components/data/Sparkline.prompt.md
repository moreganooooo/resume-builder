Compact trend line in block characters.

```jsx
<Sparkline values={scoreTrend} color="var(--tui-mauve)" />
```

Mauve for score trend, Blue for weekly volume. Downsample to the available column count before passing values in.
