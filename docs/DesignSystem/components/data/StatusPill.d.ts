export interface StatusPillProps {
  /** evaluated | applied | responded | interview | offer | skip | rejected | discarded */
  status: string;
}
export declare function StatusPill(props: StatusPillProps): JSX.Element;
