The Mission Control activity calendar — seven day-rows of `■` cells.

```jsx
<Heatmap weeks={weeks} max={maxDaily} />
```

Five steps: Surface (no activity) → Subtext → Blue → Peach → Green. Up to 52 week-columns, clipped to the terminal width.
