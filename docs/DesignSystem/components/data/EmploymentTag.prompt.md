Canonical employment type as a bracketed bold label, used on list rows and in the detail pane.

```jsx
<EmploymentTag type="contract_to_hire" />
```

Omit it entirely when the posting stated no type — a gray "Unknown" on most rows is worse than an empty slot.
