Application status as an inverted pill — Base text on the status colour.

```jsx
<StatusPill status="interview" />
```

rejected and discarded deliberately share Subtext. Pills are uppercase and square (terminals have no radius).
