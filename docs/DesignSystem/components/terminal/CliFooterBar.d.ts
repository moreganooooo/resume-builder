export interface CliFooterBarProps {
  /** nav = menu keys (display_footer_commands); execution = while a script runs. */
  variant?: "nav" | "execution";
}
export declare function CliFooterBar(props: CliFooterBarProps): JSX.Element;
