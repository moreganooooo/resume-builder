The top bar of every dashboard screen — Surface fill, gradient title left, live counts right.

```jsx
<TuiHeaderBar icon="pipeline" title="✦ CAREER PIPELINE ✧" gradient="pipeline"
  info="128 offers | Avg 3.7/5" />
```

Only surface filter state in `info` once the user has changed something — a permanent "Sort: Score" readout just restates the defaults.
