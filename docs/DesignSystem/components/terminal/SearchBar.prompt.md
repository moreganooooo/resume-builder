Live substring filter over company / role / notes.

```jsx
<SearchBar active query="creek" matched={3} total={128} />
```

Filtering is live per keystroke. Esc clears the query first and only leaves the screen on a second press.
